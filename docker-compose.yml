services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      - PORT=8080

  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    depends_on:
      - api
    networks:
      - default


